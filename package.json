{
  "name": "news-pulse",
  "version": "1.0.0",
  "private": true,
  "description": "News Pulse - RSS news aggregator with source health reporting",
  "scripts": {
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "test:report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  }
}
